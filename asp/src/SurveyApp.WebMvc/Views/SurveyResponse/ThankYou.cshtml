
@model SurveyApp.WebMvc.Models.ThankYouViewModel

@{
    ViewData["Title"] = "Gracias por su Respuesta";
}

<div class="min-h-screen flex flex-col bg-gradient-to-b from-blue-50 to-white">
    <div class="w-full max-w-4xl mx-auto px-4 py-16">
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <div class="text-center p-8 border-b border-gray-200">
                <div class="mx-auto mb-6 w-16 h-16 flex items-center justify-center rounded-full bg-green-100">
                    <svg class="w-8 h-8 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                </div>
                <h1 class="text-3xl font-bold text-gray-800 mb-3">¡Gracias!</h1>
                <p class="text-lg text-gray-600">@Model.ThankYouMessage</p>
            </div>

            <div class="p-8">
                <div class="mb-6">
                    <h2 class="text-xl font-semibold text-gray-800 mb-4">¿Qué sigue?</h2>
                    <p class="text-gray-600">
                        Su respuesta ha sido registrada con éxito. Utilizaremos esta información para mejorar 
                        nuestros servicios y productos. Si proporcionó su correo electrónico, es posible que 
                        nos comuniquemos con usted para obtener más detalles o para invitarlo a participar 
                        en futuras encuestas.
                    </p>
                </div>

                @if (Model.RelatedSurveys.Any())
                {
                    <div class="border-t border-gray-200 pt-6">
                        <h2 class="text-xl font-semibold text-gray-800 mb-4">Encuestas relacionadas</h2>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            @foreach (var survey in Model.RelatedSurveys)
                            {
                                <div class="bg-gray-50 rounded-lg p-4 hover:bg-gray-100 transition-colors">
                                    <h3 class="font-medium text-lg text-gray-800 mb-2">@survey.Title</h3>
                                    <p class="text-gray-600 text-sm mb-3">@survey.Description</p>
                                    <div class="flex justify-between items-center">
                                        <span class="text-xs bg-blue-100 text-blue-800 px-2 py-1 rounded">@survey.Category</span>
                                        <a href="/respond/@survey.Id" class="text-blue-600 hover:text-blue-800 text-sm font-medium">
                                            Participar →
                                        </a>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }

                <div class="flex justify-center mt-8">
                    <a href="/" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-6 rounded transition-colors">
                        Volver al Inicio
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
